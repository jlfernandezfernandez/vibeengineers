---
import { BASE, REPO_URL, SITE_TITLE } from '../consts';
import HeaderLink from './HeaderLink.astro';
---

<header>
	<nav>
		<h2>
			<a href={`${BASE}/`}><span class="prompt">$</span> {SITE_TITLE}</a>
		</h2>
		<div class="internal-links">
			<HeaderLink href="/">Inicio</HeaderLink>
			<HeaderLink href="/articles">Artículos</HeaderLink>
			<HeaderLink href="/how-it-works">Cómo funciona</HeaderLink>
			<a class="propose-link" href={`${REPO_URL}/issues/new/choose`} target="_blank">Proponer tema</a>
		</div>
		<div class="social-links">
			<a href={REPO_URL} target="_blank">
				<span class="sr-only">Repositorio en GitHub</span>
				<svg viewBox="0 0 16 16" aria-hidden="true" width="32" height="32"
					><path
						fill="currentColor"
						d="M8 0C3.58 0 0 3.58 0 8c0 3.54 2.29 6.53 5.47 7.59.4.07.55-.17.55-.38 0-.19-.01-.82-.01-1.49-2.01.37-2.53-.49-2.69-.94-.09-.23-.48-.94-.82-1.13-.28-.15-.68-.52-.01-.53.63-.01 1.08.58 1.23.82.72 1.21 1.87.87 2.33.66.07-.52.28-.87.51-1.07-1.78-.2-3.64-.89-3.64-3.95 0-.87.31-1.59.82-2.15-.08-.2-.36-1.02.08-2.12 0 0 .67-.21 2.2.82.64-.18 1.32-.27 2-.27.68 0 1.36.09 2 .27 1.53-1.04 2.2-.82 2.2-.82.44 1.1.16 1.92.08 2.12.51.56.82 1.27.82 2.15 0 3.07-1.87 3.75-3.65 3.95.29.25.54.73.54 1.48 0 1.07-.01 1.93-.01 2.2 0 .21.15.46.55.38A8.012 8.012 0 0 0 16 8c0-4.42-3.58-8-8-8z"
					></path></svg
				>
			</a>
		</div>
	</nav>
</header>
<style>
	header {
		position: sticky;
		top: 0;
		z-index: 10;
		margin: 0;
		padding: 0 1em;
		font-family: var(--font-mono);
		font-size: 0.85em;
		background: rgba(11, 14, 19, 0.88);
		backdrop-filter: blur(8px);
		border-bottom: 1px solid rgb(var(--gray-light));
		box-shadow: 0 8px 30px rgba(0, 0, 0, 0.12);
	}
	h2 {
		margin: 0;
		font-size: 1em;
	}
	.prompt {
		color: var(--accent);
	}
	h2 a {
		text-decoration: none;
		transition: color 160ms ease;
	}
	nav {
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 1em;
		max-width: 880px;
		margin: auto;
	}
	h2 a {
		white-space: nowrap;
	}
	/* One single row always: links scroll horizontally if they don't fit. */
	.internal-links {
		display: flex;
		min-width: 0;
		overflow-x: auto;
		white-space: nowrap;
		scrollbar-width: none;
	}
	.internal-links::-webkit-scrollbar {
		display: none;
	}
	nav a {
		padding: 1em 0.5em;
		color: rgb(var(--black));
		border-bottom: 2px solid transparent;
		text-decoration: none;
		transition:
			color 160ms ease,
			border-color 160ms ease;
	}
	nav a:hover {
		color: var(--accent);
		text-decoration: none;
	}
	nav a.active {
		text-decoration: none;
		border-bottom-color: var(--accent);
		text-shadow: 0 0 16px rgba(255, 180, 84, 0.24);
	}
	.social-links,
	.social-links a {
		display: flex;
		align-items: center;
	}
	.social-links svg {
		width: 22px;
		height: 22px;
	}
	@media (max-width: 720px) {
		header {
			padding: 0 0.55em;
		}
		nav {
			gap: 0.35em;
		}
		nav a {
			padding-right: 0.4em;
			padding-left: 0.4em;
		}
		.internal-links {
			justify-content: flex-end;
			overflow: visible;
		}
		.propose-link,
		.social-links {
			display: none;
		}
	}
	@media (max-width: 360px) {
		header {
			font-size: 0.8em;
		}
		nav {
			gap: 0;
		}
	}
</style>
